module calculator(input [3:0] btn, input [15:0] sw, output [15:0] led);
  reg [15:0] a, b;
  always @ (posedge btn[0]) begin
    a = {sw[15:0]};
  end
  always @ (posedge btn[1]) begin
    b = {sw[15:0]};
  end
  
  assign led = a + b;
  
endmodule